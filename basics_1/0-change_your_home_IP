#!/usr/bin/env bash
# Configure Ubuntu server

cp /etc/hosts /tmp/hosts

sed 's/127.0.0.1 localhost/127.0.0.2 localhost/' /tmp/hosts > /tmp/hosts.new

cat /tmp/hosts.new > /etc/hosts

echo "8.8.8.8 facebook.com" >> /etc/hosts
